plugins {
    id 'frozenlands.runtime-module'
}

dependencies {
    api(libs.sky.simulation) {
        exclude group: 'org.jmonkeyengine'
        because 'FrozenLands owns the validated JME runtime line through :core constraints.'
    }
}
