{
  "schema": "frozenlands.module.index.v1",
  "module": "engine:modules:terrain",
  "modulePath": "engine:modules:terrain",
  "id": "engine.terrain",
  "type": "runtime",
  "description": "Terrain runtime module: chunks, height, spawn and terrain config.",
  "lua": {
    "api": "lua/api.lua",
    "events": "lua/events.lua",
    "usage": "local m = require('engine.terrain'); m.call('status', {})"
  },
  "runtime": {
    "configs": {
      "runtime": "assets/config/runtime.json"
    },
    "assetRoots": [
      "assets"
    ]
  }
}
