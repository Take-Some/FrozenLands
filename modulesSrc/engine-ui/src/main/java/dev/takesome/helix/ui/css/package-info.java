/**
 * Minimal CSS bridge for HELIX UI DOM.
 *
 * <p>This package parses CSS-like stylesheets, resolves :root variables and
 * var(...) expressions, writes computed styles into UiDomElement, and resolves
 * px/% bounds into layout boxes.</p>
 */
package dev.takesome.helix.ui.css;
