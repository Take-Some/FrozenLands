/**
 * HELIX UI DOM: engine-owned retained document tree for markup UI.
 *
 * <p>This package is intentionally independent from browser DOM and from GDX.
 * It is the shared model that CSS cascade, layout, binding and render backends
 * will consume.</p>
 */
package dev.takesome.helix.ui.dom;
