package dev.takesome.helix.ui.uiComponents.input;

/** Runtime state for a key/input capture placeholder. */
public enum UiInputCaptureState {
    IDLE,
    HOVERED,
    LISTENING,
    CAPTURED,
    CANCELLED,
    DISABLED
}
