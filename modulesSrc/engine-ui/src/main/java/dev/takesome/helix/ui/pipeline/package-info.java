/**
 * UI document rendering pipeline passes. The public renderer remains a facade;
 * layout, binding preparation, panel rendering and widget rendering are split
 * into small explicit passes.
 */
package dev.takesome.helix.ui.pipeline;
