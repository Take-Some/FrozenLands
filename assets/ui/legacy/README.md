# Legacy UI assets

This directory is reserved for migration notes. Existing legacy runtime files still live in `forms/` and `icons/` until the Java UI code is moved to HtmlDom.

Do not add new UI screens here.
